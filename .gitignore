# Node
server/node_modules/
server/dist/
*.log

# Android / Gradle
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.iml

# OS
.DS_Store

# Local tooling
.claude/

# Signing keys. The release keystore is intentionally committed (open-source,
# personal LAN tool — see android/app/agentshud-release.jks); ignore any others.
*.jks
*.keystore
*.jks.base64
!android/app/agentshud-release.jks

# Kotlin compiler session caches
android/.kotlin/
